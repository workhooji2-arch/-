/**
 * Shown when the app is reachable but a required environment variable is
 * missing, so a session cannot be issued. Points at the page that names the
 * missing piece rather than leaving a blank error screen.
 */
export const SETUP_INCOMPLETE =
  "서버 설정이 끝나지 않아 로그인할 수 없습니다. /setup-check 페이지에서 빠진 항목을 확인해주세요.";
